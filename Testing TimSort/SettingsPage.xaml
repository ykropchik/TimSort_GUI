<Page
    x:Class="Testing_TimSort.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource SystemControlPageBackgroundAltMediumBrush}">
    
    <Grid>
        <ScrollViewer
            Padding="56,0,0,0"
            IsTabStop="False"
            UseSystemFocusVisuals="False"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock
                    Style="{StaticResource SubtitleTextBlockStyle}"
                    Margin="0,12,0,0"
                    Text="Настройки" />
                <StackPanel x:Name="ThemePanel" Margin="0,10,0,0">
                    <RadioButton Tag="Light" Checked="OnThemeRadioButtonChecked" Content="Светлая" />
                    <RadioButton Tag="Dark" Checked="OnThemeRadioButtonChecked" Content="Темная" />
                    <RadioButton Tag="Default" Checked="OnThemeRadioButtonChecked" Content="Использовать настройки системы" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
    
</Page>
